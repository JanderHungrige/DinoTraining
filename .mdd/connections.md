---
generated: 2026-08-17
doc_count: 2
connection_count: 0
overlap_count: 0
---

# Connections

Auto-generated by `/mdd`. Do not edit by hand — it is rewritten on every feature completion.

## Path Tree

```
Meta
└── Schema  00-frontmatter-spec  complete
Platform
└── Shell  01-app-shell  complete
```

## Dependency Graph

```mermaid
graph TD
  spec["00-frontmatter-spec"]:::complete
  shell["01-app-shell"]:::complete

  classDef complete fill:#00e5cc,color:#000
  classDef in_progress fill:#ffaa00,color:#000
  classDef draft fill:#888,color:#fff
  classDef deprecated fill:#555,color:#aaa
```

`01-app-shell` has no dependencies — it is the root of the Wave 1 graph. The remaining
Wave 1 features all depend on it:

```mermaid
graph TD
  shell["01-app-shell"]:::complete
  models["model-manager"]:::draft
  store["dataset-store"]:::draft
  annot["grounding-dino-annotator"]:::draft
  canvas["annotation-canvas"]:::draft
  flow["annotation-workflow"]:::draft

  shell --> models
  shell --> store
  shell --> canvas
  models --> annot
  store --> annot
  canvas --> flow
  annot --> flow
  store --> flow

  classDef complete fill:#00e5cc,color:#000
  classDef draft fill:#888,color:#fff
```

## Source File Overlap

No source file is referenced by two or more docs yet.

## Warnings

None. No broken `depends_on` references, no cycles, every doc has a `path`.
