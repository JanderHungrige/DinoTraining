import type { JSX } from 'react';

import { StubPanel } from '../components/StubPanel';

/** Wave 1 — replaced by model-manager (feature 02). */
export function AdminTab(): JSX.Element {
  return <StubPanel tabId="admin" />;
}
