import type { JSX } from 'react';

import { StubPanel } from '../components/StubPanel';

/** Wave 4 — dataset generator UI. */
export function DatasetGeneratorTab(): JSX.Element {
  return <StubPanel tabId="generator" />;
}
