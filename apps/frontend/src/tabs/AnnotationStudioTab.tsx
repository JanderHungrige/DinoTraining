import type { JSX } from 'react';

import { StubPanel } from '../components/StubPanel';

/** Wave 1 — replaced by annotation-workflow (feature 06). */
export function AnnotationStudioTab(): JSX.Element {
  return <StubPanel tabId="studio" />;
}
